# Practice 1 — Python Basics

Examples for the W3Schools Python topics from **HOME** through **Python Strings**.

Run any example from the repository root with Git Bash:

```bash
python Practice1/python-basics/hello_world.py
```

Each file is small, self-contained, and uses only Python's standard features.
