# Practice2

Python control-flow examples based on the W3Schools topics from Boolean Values through For Loops.

Run an example from the repository root:

```bash
python Practice2/loops/for_loop.py
```

The examples are organized into `boolean`, `if_else`, and `loops`.
